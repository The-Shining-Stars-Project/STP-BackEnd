# The review sheet and any CSV export of it contain real children's medical details,
# guardian contacts and dates of birth. They are working files for one migration and must
# never enter version control. The tools that PRODUCE them are tracked; their output is not.
*.xlsx
*.csv
